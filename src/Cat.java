public class Cat extends Zoo {
    Cat(String name, int weight, int growth, String voice) {
        setName(name);
        setWeight(weight);
        setGrowth(growth);
        setVoice(voice);
    }
}
