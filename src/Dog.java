public class Dog extends Zoo {
    Dog(String name, int weight, int growth, String voice) {
        setName(name);
        setWeight(weight);
        setGrowth(growth);
        setVoice(voice);
    }
}
